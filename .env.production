# Production environment settings
ENV=production
DOCKERFILE_PATH=Dockerfile
# Don't mount code in production to use the containerized version
MOUNT_CODE=
# Disable reload in production
RELOAD=
# Set Python path for proper module imports
PYTHONPATH=/app
# Backend connection settings for frontend
BACKEND_HOST=backend
BACKEND_PORT=8000
